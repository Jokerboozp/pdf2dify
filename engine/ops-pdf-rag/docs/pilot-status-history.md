# 实施与验收状态

**当前全量任务：** 257 份 PDF、5,244 页已完成本地处理，九个主题库的 4,840 个当前父文档全部完成索引。全量远端核对页码、来源元数据、8,084 处原图和4,561条检索子块通过，52条旧章节已标记失效。29项代码测试通过。工作流 #6 已发布；详细功能验收见 [全量验收记录](acceptance.md)。以下为前期试点记录。

2026-09-16，本机工程 `D:\rag\ops-pdf-rag`，目标实例 Dify 1.17.1。

后续图文更新：累计处理83页，44条问答已改为图文入库，其中43条含77张截图，原TXT停用保留。回答展示图片已通过实际浏览器放大验证。下文“50页试点”与无图描述记录的是初版范围；最新状态见 [图文接入说明](images.md)。

## 已完成

全库文件清点、全部 5,244 页原生文字提取、50 页本地 OCR、50 条 FAQ 草稿、6 份跨页流程包、44 条清洗问答实际入库、可导入 Chatflow 和真实浏览器问答联调。处理日志和评测结果在 `data/reports`。

一键试点脚本复跑成功：50 页全部命中缓存，0 页失败。原文事务码和否定条件保持不变；FAQ 标签即使在 PDF 对象顺序中颠倒，仍按“问题描述/原因/解决办法”归位。

本机 CPU OCR 对筛除装饰图后的试点首次处理约 180 秒，后续报告中的约 9 秒包含 OCR 缓存，不能据此估算全量冷处理耗时。全量手册的页面密度、表格和图片大小不同，需按批次实测。

## 测试口径

`evaluation/cases.yaml` 有 60 个用例，其中 40 个问题已有入库文字依据，10 个属于后续流程/截图覆盖，10 个用于澄清或拒绝无依据回答。离线评测只计算前 40 个的检索命中：Top 1 为 39/40，Top 5 为 40/40。用例由资料整理产生，尚待业务负责人审定；不能解释成上线准确率。

Chatflow 最终版 6 次真实联调结果单独记录在 `live-checks.md`。13 项代码测试通过，覆盖字段顺序、否定条件与数字保留、溯源、检索区别、API 分页与错误处理、无沙箱工作流及分批续跑。代码测试结果不替代生产验收。

## 已发现并处理的问题

1. 代码节点触发 DifySeccomp 错误，无法运行。改用 Dify 内置结构化输出，删除代码节点，未修改服务器安全设置。
2. 首次回答附带了另一页的相似故障处理方法。回答提示词改为只回答本轮对象、动作、条件匹配的内容，复测消除了该段串答。
3. 多轮问题整理曾将 JSON 示例占位符“独立检索问题”作为 query 返回。删除占位符示例，增加真实指代示例和当前任务指令；复测结果见 live-checks.md。

## 仍待完成

- 257 份 PDF 的全量截图 OCR 与通用流程切分：当前仅完成试点 50 页 OCR，原生文字已全量完成。
- 截图动作、红框、箭头、表格合并关系的业务核对；当前 309 行低置信 OCR 标记待查，不是 309 个已确认错误。
- 44 条试点问答的适用系统版本、岗位权限和业务时效确认；6 条排除问答与 6 份流程包的修订。
- 60 个用例的完整 Dify 端到端验收，以及真实用户问题扩充；真实联调仅为小样本验证。
- 长流程父子分段、模块/版本元数据过滤与重排模型。目前 FAQ 普通分段、无重排；回答配图已接入 Dify 自身文件存储。
- API 增量同步在目标实例的凭据配置和实际验收。目前已通过 UI 完成试点入库，没有 API 密钥。

扩展顺序：先解决复核清单 → 每次扩展一个模块并建立独立测试集 → 根据漏召回和串答调整分段/向量/关键词/重排 → 通过业务验收后扩大使用范围。
